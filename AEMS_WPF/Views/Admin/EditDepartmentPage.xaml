<Page x:Class="AEMS_WPF.Views.Admin.EditDepartmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Cập nhật phòng ban" Background="#F4F6F9" FontFamily="Segoe UI"
      d:DesignHeight="900" d:DesignWidth="1200">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="4" Opacity="0.03"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
        </Grid.LayoutTransform>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40,30">
            <StackPanel HorizontalAlignment="Center" Width="700">

                <StackPanel Margin="0,0,0,30">
                    <Button Content="← quay lại danh sách" Background="Transparent" Foreground="#64748B" BorderThickness="0" Padding="0" FontWeight="SemiBold" FontSize="14" Cursor="Hand" Click="BtnBack_Click" HorizontalAlignment="Left" Margin="0,0,0,15"/>
                    <TextBlock Text="Cập nhật phòng ban" FontSize="28" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,5"/>
                    <TextBlock Text="Chỉnh sửa thông tin chi tiết của phòng ban trong hệ thống." FontSize="14" Foreground="#64748B"/>
                </StackPanel>

                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="35,30">
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Tên phòng ban" Style="{StaticResource LabelStyle}" Margin="0,0,20,0"/>
                                <TextBox x:Name="txtDepartmentName" Grid.Column="1" Style="{StaticResource InputStyle}"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Mã phòng ban" Style="{StaticResource LabelStyle}" Margin="0,0,20,0"/>
                                <TextBox x:Name="txtDepartmentCode" Grid.Column="1" Style="{StaticResource InputStyle}"/>
                            </Grid>
                        </StackPanel>

                        <Border Grid.Row="1" Height="1" Background="#E2E8F0"/>

                        <Border Grid.Row="2" Background="#F8FAFC" Padding="30,20" CornerRadius="0,0,12,12">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Hủy bỏ" Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20,10" FontWeight="SemiBold" FontSize="14" Cursor="Hand" Click="BtnCancel_Click" Margin="0,0,12,0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button Content="Cập nhật phòng ban" Background="#3B82F6" Foreground="White" BorderThickness="0" Padding="20,10" FontWeight="SemiBold" FontSize="14" Cursor="Hand" Click="BtnUpdate_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>