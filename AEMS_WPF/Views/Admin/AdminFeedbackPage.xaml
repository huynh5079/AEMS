<Page x:Class="AEMS_WPF.Views.Admin.AdminFeedbackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Feedback Management" Background="#F5F7FA" d:DesignHeight="900" d:DesignWidth="1200">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20" Orientation="Horizontal">
            <Button Click="BtnBack_Click" Background="Transparent" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand" VerticalAlignment="Top">
                <TextBlock Text="←" FontSize="26" FontWeight="Bold" Foreground="#A1A5B7" ToolTip="Back to Dashboard Overview"/>
            </Button>

            <StackPanel>
                <TextBlock Text="Feedback Management" FontSize="26" FontWeight="ExtraBold" Foreground="#1E1E2D"/>
                <TextBlock Text="Overview and manage all feedback across the system" Foreground="#A1A5B7" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Total Feedback" FontSize="14" FontWeight="SemiBold" Foreground="#A1A5B7"/>
                        <TextBlock Text="{Binding TotalCount}" FontSize="28" FontWeight="Bold" Foreground="#1E1E2D" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#F1FAFF" Width="48" Height="48" CornerRadius="10">
                        <TextBlock Text="📝" FontSize="20" Foreground="#009EF7" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Avg Rating" FontSize="14" FontWeight="SemiBold" Foreground="#A1A5B7"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{Binding AverageRating}" FontSize="28" FontWeight="Bold" Foreground="#1E1E2D"/>
                            <TextBlock Text=" / 5.0" FontSize="14" FontWeight="SemiBold" Foreground="#A1A5B7" VerticalAlignment="Bottom" Margin="2,0,0,5"/>
                        </StackPanel>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#FFF8DD" Width="48" Height="48" CornerRadius="10">
                        <TextBlock Text="⭐" FontSize="20" Foreground="#FFC700" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="20" Margin="0,0,15,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Positive Feedback" FontSize="14" FontWeight="SemiBold" Foreground="#A1A5B7"/>
                        <TextBlock Text="{Binding PositiveCount}" FontSize="28" FontWeight="Bold" Foreground="#1E1E2D" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#E8FFF3" Width="48" Height="48" CornerRadius="10">
                        <TextBlock Text="👍" FontSize="20" Foreground="#50CD89" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="3" Background="White" CornerRadius="12" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Negative Feedback" FontSize="14" FontWeight="SemiBold" Foreground="#A1A5B7"/>
                        <TextBlock Text="{Binding NegativeCount}" FontSize="28" FontWeight="Bold" Foreground="#1E1E2D" Margin="0,8,0,0"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#FFF5F8" Width="48" Height="48" CornerRadius="10">
                        <TextBlock Text="👎" FontSize="20" Foreground="#F1416C" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <ComboBox x:Name="cbRatingFilter" Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-65,0,0" 
                      Width="180" Height="40" Background="White" FontSize="14" VerticalContentAlignment="Center" Padding="10,0" Cursor="Hand"
                      SelectionChanged="CbRatingFilter_SelectionChanged">
                <ComboBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </ComboBox.Resources>
            </ComboBox>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="5" BorderBrush="#E4E6EF" BorderThickness="1">
            <DataGrid x:Name="dgvFeedbacks" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                      BorderThickness="0" Background="White" RowHeight="75" FontSize="14" GridLinesVisibility="None" 
                      SelectionMode="Single" VerticalScrollBarVisibility="Auto">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="#A1A5B7"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,15"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E4E6EF"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderBrush" Value="#F0F2F5"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Background" Value="White"/>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="EVENT" Binding="{Binding EventName}" Width="1.5*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="15,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#1E1E2D"/>
                                <Setter Property="TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="PARTICIPANT" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="15,0">
                                    <TextBlock Text="{Binding ParticipantName}" FontWeight="Bold" Foreground="#1E1E2D" FontSize="14"/>
                                    <TextBlock Text="{Binding ParticipantRole}" Foreground="#A1A5B7" FontSize="12" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="TIMING" Binding="{Binding Timing}" Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="15,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#7E8299"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="RATING" Binding="{Binding StarRating}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="15,0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#FFC700"/>
                                <Setter Property="FontSize" Value="18"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="COMMENT" Binding="{Binding Comment}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#5E6278"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="15,0"/>
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="MaxHeight" Value="60"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="DATE" Binding="{Binding Date}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#7E8299"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Margin" Value="15,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>