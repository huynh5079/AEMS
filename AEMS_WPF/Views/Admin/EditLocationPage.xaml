<Page x:Class="AEMS_WPF.Views.Admin.EditLocationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Edit Location" Background="#F0F2F5" d:DesignHeight="800" d:DesignWidth="1200">

    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#F5F8FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E4E6EF"/>
            <Setter Property="Foreground" Value="#3F4254"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#2962FF"/>
                    <Setter Property="Background" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#181C32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,25">
                <Button x:Name="btnBack" DockPanel.Dock="Left" Background="Transparent" BorderThickness="0" 
                        Cursor="Hand" Margin="0,0,20,0" VerticalAlignment="Top" Click="BtnBack_Click" ToolTip="Quay lại">
                    <Border Background="White" Width="48" Height="48" CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                        </Border.Effect>
                        <TextBlock Text="⬅" FontSize="20" Foreground="#1A237E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Button>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Edit Location" FontSize="26" FontWeight="ExtraBold" Foreground="#1A237E"/>
                    <TextBlock Text="Chỉnh sửa thông tin địa điểm tổ chức sự kiện" Foreground="#7E8299" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>
            </DockPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="30" MaxWidth="900" HorizontalAlignment="Left" Width="900">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.05"/>
                </Border.Effect>

                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Location Name *" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtName"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Capacity *" Style="{StaticResource LabelStyle}"/>
                            <TextBox x:Name="txtCapacity"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Location Type" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="cbType" Height="42" FontSize="14" Padding="10,0" BorderBrush="#E4E6EF" Background="#F5F8FA"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Status *" Style="{StaticResource LabelStyle}"/>
                            <ComboBox x:Name="cbStatus" Height="42" FontSize="14" Padding="10,0" BorderBrush="#E4E6EF" Background="#F5F8FA"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Address *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="txtAddress" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="15,10"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,30">
                        <TextBlock Text="Description" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="txtDescription" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="15,10"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="btnCancel" Content="Cancel" Width="100" Height="45" Background="Transparent" Foreground="#7E8299" 
                                FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0" Click="BtnCancel_Click"/>

                        <Button x:Name="btnSave" Content="Save Changes" Width="140" Height="45" Background="#2962FF" Foreground="White" 
                                FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand" Click="BtnSave_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>