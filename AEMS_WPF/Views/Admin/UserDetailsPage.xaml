<Page x:Class="AEMS_WPF.Views.Admin.UserDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Hồ sơ người dùng" Background="#F4F6F9" FontFamily="Segoe UI"
      d:DesignHeight="900" d:DesignWidth="1200">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
        </Grid.LayoutTransform>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="40,30">
            <StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,25" VerticalAlignment="Center">
                    <Button Content="← Quay lại" Background="White" Foreground="#475569" BorderBrush="#CBD5E1" BorderThickness="1" Padding="15,6" FontWeight="SemiBold" Cursor="Hand" Click="BtnBack_Click" Margin="0,0,15,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <TextBlock x:Name="txtHeaderTitle" Text="Hồ sơ: Hao Truong Dang Nguyen" FontSize="22" FontWeight="Bold" Foreground="#0F172A" VerticalAlignment="Center"/>
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="30" Margin="0,0,25,0" VerticalAlignment="Top">
                        <StackPanel>
                            <Border x:Name="borderAvatar" Width="120" Height="120" CornerRadius="60" Background="#E2E8F0" BorderBrush="#CBD5E1" BorderThickness="2" HorizontalAlignment="Center" Margin="0,0,0,20">
                                <TextBlock x:Name="txtInitials" Text="HN" FontSize="40" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <TextBlock x:Name="txtFullName" Text="Hao Truong Dang Nguyen" FontSize="18" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,0,0,5" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock x:Name="txtEmail" Text="haotdn@gmail.com" FontSize="14" Foreground="#3B82F6" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
                                <Border x:Name="badgeRole" Background="#64748B" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                                    <TextBlock x:Name="txtRole" Text="Student" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                </Border>
                                <Border x:Name="badgeStatus" Background="#16A34A" CornerRadius="4" Padding="8,4">
                                    <TextBlock x:Name="txtStatus" Text="Active" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>

                            <TextBlock Text="Loại khóa" FontSize="13" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,8"/>
                            <ComboBox Height="38" SelectedIndex="0" Margin="0,0,0,8" BorderBrush="#CBD5E1">
                                <ComboBoxItem Content="Khóa thủ công"/>
                                <ComboBoxItem Content="Khóa tự động"/>
                            </ComboBox>
                            <TextBlock Text="Khóa thủ công sẽ chỉ mở khi admin mở khóa." FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                            <Button Content="🔒 Khóa tài khoản" Background="#EF4444" Foreground="White" FontSize="14" FontWeight="Bold" Height="42" BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="4"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" VerticalAlignment="Top">
                        <StackPanel>
                            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="25,15" CornerRadius="8,8,0,0">
                                <TextBlock Text="Thông tin chi tiết" Foreground="#3B82F6" FontWeight="SemiBold" FontSize="15"/>
                            </Border>

                            <Grid Margin="25">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,20">
                                    <TextBlock Text="ID Hệ thống" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtSystemId" Text="-" FontSize="14" Foreground="#0F172A" TextWrapping="Wrap"/>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,20">
                                    <TextBlock Text="Ngày tham gia" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtJoinDate" Text="-" FontSize="14" Foreground="#0F172A"/>
                                </StackPanel>

                                <Border Grid.Row="1" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,20"/>

                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,15,20">
                                    <TextBlock Text="Số điện thoại" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtPhone" Text="Chưa cập nhật" FontSize="14" Foreground="#0F172A"/>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,20">
                                    <TextBlock Text="Google ID" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtGoogleId" Text="-" FontSize="14" Foreground="#0F172A" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Grid.Row="3" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,20"/>

                                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,15,20">
                                    <TextBlock x:Name="lblUserCode" Text="Mã người dùng" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtUserCode" Text="-" FontSize="14" Foreground="#3B82F6" FontWeight="SemiBold"/>
                                </StackPanel>
                                <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,0,0,20">
                                    <TextBlock x:Name="lblDepartment" Text="Phòng ban" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtDepartment" Text="-" FontSize="14" Foreground="#0F172A"/>
                                </StackPanel>

                                <Border Grid.Row="5" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,20"/>

                                <StackPanel Grid.Row="6" Grid.Column="0" Margin="0,0,15,0">
                                    <TextBlock x:Name="lblExtraInfo" Text="Thông tin thêm" FontSize="12" Foreground="#64748B" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtExtraInfo" Text="-" FontSize="14" Foreground="#0F172A"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>